#include <stdio.h>
struct Node{
    int data;
    struct Node* next;
};
void createList(){
    int k,n;
    struct Node *p,*head;
    printf("Number of nodes:");
    scanf("%d",&n);
    for(int k=0;k<n;k++){
        
    }
}